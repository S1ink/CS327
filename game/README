Parses the `$HOME/.rlg327/monster_desc.txt` and `$HOME/.rlg327/object_desc.txt` files for monster and item descriptions
and re-serializes all descriptions to the console.

**BUILD**:
    Run `make`

**USAGE**:
    Run: `./game`

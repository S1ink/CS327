#include <iostream> // iostream instantiates std::cin, std::cout, and std::cerr (stdin, stdout, stderr from C, but instances of i/ostream classes)

// "::" is called the "scope resolution operator" lol
// "<<" is the "stream insertion operator"

int main(int argc, char** argv)
{
    std::cout << "你们好" << std::endl;

    return 0;
}
